numbers = [float(x) for x in input().split()]
dict_of_numbers = {}


for num in numbers:
    if num not in dict_of_numbers:
        dict_of_numbers[num] = 0
    dict_of_numbers[num] += 1

for number, count in dict_of_numbers.items():
    print(f"{number:.1f} - {count} times")
